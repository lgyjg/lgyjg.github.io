# INTRO

This repo is my personal blog at [www.aman.site](http://www.aman.site), if you are intresting it, you can visit my blog pages.

# INSTALL
```shell
sudo apt-get install ruby-dev
sudo gem install jekyll github-pages bundler
```

# DEBUG AT LOCAL
```shell
# use jekll
jekyll serve -w
# use bundle
bundle exec jekyll serve
```